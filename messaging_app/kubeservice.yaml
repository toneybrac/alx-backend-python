apiVersion: v1
kind: Service
metadata:
  name: django-main-service
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  selector:
    app: django-app
    version: blue  # Start with blue as active version
  type: LoadBalancer
